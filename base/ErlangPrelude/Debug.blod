module ErlangPrelude.Debug

-- TODO: Is this okay? Should ErlangPrelude be moved into modules in this folder?
import ErlangPrelude

export
erlPrintLn : a -> IO ()
erlPrintLn x = do
  erlCall "io" "format" ["~p~n", the (List _) [MkRaw x]]
  pure ()

export
erlInspect : a -> a
erlInspect x = unsafePerformIO $ do
  erlPrintLn x
  pure x
